{
  "name": "blackjack-vite",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "pwsh -c mv dist docs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.15"
  },
  "dependencies": {
    "underscore": "^1.13.6"
  }
}
